"""Equipment-side simulators for the machine vision POC.

The simulators have no real I/O; they only print structured log lines so the
sequence controller can be tested without a PLC, robot, or camera attached.
"""
